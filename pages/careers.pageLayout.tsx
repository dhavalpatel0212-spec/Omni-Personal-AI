import { AppLayout } from '../components/AppLayout';

// This page uses the AppLayout, which will show the minimal version for unauthenticated users
// and the full app chrome for authenticated users, providing a consistent experience.
export default [AppLayout];